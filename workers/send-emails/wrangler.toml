name = "cairn-send-emails"
main = "index.js"
compatibility_date = "2026-08-01"

# Hourly. Each run only writes to people whose local clock has just
# reached the sending hour, so this is not an hourly mailshot.
[triggers]
crons = ["0 * * * *"]
